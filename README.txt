Agents Chat Portable（Agents 群聊）Windows 版
================================================

一、这是什么
-----------
一个免安装的多智能体群聊工具：
- 复用你电脑上已安装的 OpenCode（opencode 命令）作为统一内核，
  具备读写文件、执行命令、联网检索等真实能力
- 内置 3 个智能体（可多开可自定义）：规划师 / 工程师 / 研究员，
  每个实例可以有不同的角色人设
- 微信风格群聊界面，@智能体ID 点名对话
- 任务队列：粘贴任务清单自动提取，按时间先后排序，逐个执行，
  各任务之间上下文完全独立（每次执行都是全新会话）
- 真实反馈：执行过程中的每一次工具调用（读文件、跑命令等）都会
  实时显示在聊天流里；失败会明确标红报错，绝不假装完成

二、前置要求（必读）
--------------------
1. 本机已安装 OpenCode，cmd 里能跑通：
       opencode --version
   未安装的话先安装：
       npm install -g opencode-ai
2. OpenCode 里已配置好模型（终端运行 opencode auth login，
   按提示选择服务商并登录）。模型与密钥由 OpenCode 自己管理，
   本程序不做任何 API 配置。

三、快速开始
-----------
1. 解压 zip 到任意目录（路径不要含特殊字符）
2. 双击 start.bat，浏览器自动打开 http://localhost:3456
3. 右上角显示绿色「OpenCode 内核」= 一切正常；
   红色「未检测到 OpenCode」= 先完成上面第二节
4. 左上角「导入」粘贴任务清单，或直接在聊天框对话

四、可选配置（.env 文件）
------------------------
一般不需要改，全部有默认值：

  AGENTS_CHAT_MODEL=              指定模型（provider/model 格式），
                                  留空用 OpenCode 默认模型
  AGENTS_CHAT_AUTO_APPROVE=1      自动批准权限请求（无人值守执行需要）
  AGENTS_CHAT_TIMEOUT_MS=600000   单任务超时（毫秒）
  AGENTS_CHAT_OPENCODE_CMD=       手动指定 opencode 完整路径
  AGENTS_CHAT_MOCK=0              1=演示模式（模拟输出，界面体验用）

五、界面使用
-----------
【聊天】
  - 输入框直接说话 → 由「管家」理解需求、调度子智能体完成
  - @工程师 你好 → 点名某个智能体
  - @工程师 @研究员 做个调研 → 多个智能体流水线依次执行
  - 管家调度：规划 → 派发执行（阶段内并行/阶段间串行）→ 验收
    （不合格打回重做）→ 汇总交付
  - 汇总包含简明结果概要；有成果文件时列出完整路径
  - 子智能体过程内容默认折叠（约三行），点「展开全文」查看
  - 每个智能体执行时显示「正在执行…」动画条
  - [工具] read（文件路径）执行完成 = 真实工具调用（含操作对象）
  - 处理中「发送」按钮变红色「停止」，点击可随时中止当前编排
  - 每条正式产出下方显示「📄 完整产出文件」路径：产出全文已存档，
    后续智能体会直接读取完整版
  - 任务批量执行期间仍可正常聊天，两边互不阻塞

【任务队列（左侧）— 顺序任务】
  - 「导入任务」→「顺序任务」：每行一个任务，使用 1. 2. 3. 编号
  - 行末可 @智能体 指派（输入 @ 有自动补全）：
      1. 检查邮件 @管家     → 管家调度多智能体完成
      2. 写周报 @文案       → 文案独立完成
  - 按编号顺序执行；点单个任务的「执行」跑一个；
    底部按钮按顺序跑全部待办；列表可拖拽调序
  - 任务互不干扰：每个任务都是独立会话，上下文不互通
  - 状态：灰=待执行，蓝=执行中，绿=完成，红=失败（点开可看报错）

【定时任务（⏰ 按钮）】
  - 「导入任务」→「定时任务」：行首写启动时间（格式 20260818-1307）
  - 行首输入 [] 自动填入当前日期时间，可自行修改
  - 到点自动执行（需程序保持运行）；所有定时任务在左侧
    「⏰ 定时任务」按钮中统一管理（倒计时/状态/立即执行/删除）
  - 未触发的定时任务不出现在任务会话列表，触发后自动出现

【智能体配置（⚙）】
  - 每个智能体可选职业图标，聊天头像/成员栏/补全菜单统一显示
  - 统一工作目录：所有智能体共用一处读写文件（配置页顶部设置）

【稳定性说明】
  - 程序启动/退出时自动把「执行中」的任务复位为「待执行」，
    异常关机不会留下永远卡住的任务
  - 同一批任务不会被重复启动（防双击/防并发重复执行）
  - 若浏览器提示「与服务连接中断」，任务可能仍在后台执行，
    刷新页面即可看到最新进度

六、常用问题
-----------
Q: 换端口？
A: 编辑 start.bat，把 set PORT=3456 改成别的

Q: 怎么加/改智能体？
A: 网页右上角「⚙ 智能体配置」：名称（@点名用）、职业图标、
   技能说明（管家调度参考）、自定义提示词。统一工作目录在配置页
   顶部设置，所有智能体共用。

Q: 任务失败显示超时？
A: 在 .env 里调大 AGENTS_CHAT_TIMEOUT_MS（默认 10 分钟）

Q: 数据在哪？怎么清空？
A: 全部在 .data/ 目录（聊天记录、任务、配置）。
   网页里有「清空」按钮；删除 .data/ 目录 = 恢复出厂

Q: 双击 start.bat 闪退？
A: 打开 cmd，cd 进目录手动运行 bin\node.exe app\server.js 看报错；
   .data/server.log 里也有完整日志

七、目录说明
-----------
  .env            可选行为配置（见第四节）
  start.bat       双击启动
  bin/node.exe    内置 Node 22 运行时（跑本程序自身，免安装）
  app/            应用代码（零依赖）
  .data/          运行数据（可随时清空）
  .data/outputs/  每次对话/任务中各智能体的完整产出归档
                  （按会话分目录，串行交接时后续智能体直接读取完整文件，
                  界面消息下方的「📄 路径」即指向这里）
